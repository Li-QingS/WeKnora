# 最终提交邮件

## 邮件标题

【犀牛鸟实战提交】【课题三】【李青松 / Li-QingS】

## 邮件正文

姓名：李青松  
GitHub ID：Li-QingS  
选题编号和题目：课题三——质量评测基线与成本可观测  
成果类型：代码  
成果链接：https://github.com/Li-QingS/WeKnora/tree/rhino-2026-final-3  
代码版本（如有）：Tag `rhino-2026-final-3`；Commit SHA `501f957e584f99f1d231466fa27fa0c0ccd25980`  
运行或阅读说明：请先阅读成果目录中的 `docs/submission/README.md`。开发环境执行 `make dev-start`，再分别运行 `make dev-app` 和 `make dev-frontend`；配置模型和访问令牌后，执行 `make eval-baseline CONFIG=./evaluation/configs/enterprise.yaml` 可生成 JSON 与 Markdown 评测报告。Web 端可在“设置 → 评测中心 / 模型用量”查看 RAG/Wiki 评测和模型调用、缓存、费用数据。  
完成情况和已知问题：已完成评测配置及结果持久化、四类指标报告、一键 Runner、RAG CI 质量门禁、模型调用台账与费用估算、Embedding 缓存、Wiki Prompt Cache 优化、解析引擎横向基线，以及统一评测中心和模型用量页面。Embedding 固定场景命中率由 33.33% 提升至 66.67%，Provider 输入减少 50%；Wiki 引用抽取热运行缓存读取率由 9.83% 提升至 15.34%。Wiki 评测属于手动实验功能，不作为 CI 门禁；Embedding 缓存默认关闭且尚未提供长期大规模生产所需的完整容量配额与 TTL 治理；模型费用需预先配置单价；真实模型 CI 需配置仓库 Secrets 和分支保护。详细复现步骤、统计口径和证据路径见 `docs/submission/README.md`。

## 随邮件确认

- 代码仓库为公开仓库，无需额外申请权限。
- 最终 Tag 已创建并推送，不移动、不覆盖。
- `submission.yaml` 位于仓库根目录。
- 根目录 `README.md` 和 `docs/submission/README.md` 提供部署、运行、测试与证据说明。
- `docs/submission/WeKnora_课题三_功能展示与验收手册_李青松.docx` 可作为最终成果附件，包含逐功能展示、操作验收和缓存实测对照。
