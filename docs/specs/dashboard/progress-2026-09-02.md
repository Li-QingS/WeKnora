# 开发进展记录（2026-09-02）

## 工作包

- 课题：腾讯犀牛鸟 · WeKnora 开源训练营 课题三「质量评测基线与成本可观测」
- 工作包：WP7 前端展示与演示收尾
- 分支：`feat/evaluation-persistence`
- 状态：开发完成，真实 API 验收通过

## 实现内容

### 评测中心

- 新增 `frontend/src/api/evaluation/index.ts`：
  - `listEvaluationRuns(page, pageSize, status?)` → `GET /api/v1/evaluation/runs`
  - `getEvaluationResult(taskId)` → `GET /api/v1/evaluation?task_id=...`
  - `EvaluationRun / EvaluationMetric / EvaluationConfigSnapshot / EvaluationDetail` 类型
- 新增 `frontend/src/views/settings/EvaluationCenterSettings.vue`：
  - 设置页 `.section-header` 结构 + TDesign 表格/标签/分页/空态/加载
  - 点击历史运行行加载详情，展示运行 ID、数据集、样本数、状态、`config_hash`、错误信息
  - 展示 12 项指标（检索 6 + 生成 6）、模型快照与版本信息

### 模型用量页缓存统计

- `frontend/src/views/settings/ModelUsageSettings.vue` 顶部新增“Embedding 缓存”区块：
  - `hits / misses / provider_calls`
  - 请求失败或无数据时显示 `unknown`
- `frontend/src/api/model/usage.ts` 新增 `EmbeddingCacheStats` 与 `getEmbeddingCacheStats()`

### 设置页注册与权限

- `frontend/src/views/settings/Settings.vue`：
  - 导入并渲染 `EvaluationCenterSettings`
  - 导航增加“评测中心”，放入 `models_runtime` 分组
- `frontend/src/config/settingsAccess.ts` 增加 `evaluation: 'viewer'`，与后端 `g.Viewer()` 路由一致

## 自动化验证

- `cd frontend && npm run type-check` 通过
- `cd frontend && npm run build` 通过
- `go build ./...` 通过
- `go test ./internal/...` 通过
- `cd client && go test ./...` 通过
- `cd cli && go test ./...` 与 `go vet ./...` 通过

## 真实 API 验证

本地启动 Lite 后端与现有 PG 容器，临时插入一条成功运行的 `evaluation_runs` 后：

```text
list_success=True total=1 id=evaluation_1_wp7_e2e_demo metric_count=12 sample_count=1
detail_task=evaluation_1_wp7_e2e_demo has_metric=True
cache_success=True data={'hits': 0, 'misses': 0, 'provider_calls': 0}
```

三个前端封装对应的真实接口均返回成功，字段与前端类型一致；测试运行记录和临时账号已从数据库清理。

## 已知未覆盖项

- 未做浏览器截图式目视复核；视觉一致性通过组件代码、TDesign 类名、CSS 变量与构建产物检查完成。
- 状态筛选下拉未做（Spec 中为可选）。
- 图表、评测发起按钮、价格编辑仍不在 WP7 范围。

## 下一步建议

- 用户打开设置页“评测中心 / 模型用量”做最终目视确认。
- 进入最终演示与课题材料整理阶段（报告、截图、录像、PPT 素材）。
