# Wiki 缓存证据更新：2026-09-08 与 2026-09-10 最终版本对照

## 结论

2026-09-08 目录保存优化前参考数据。2026-09-10 版本加入 Provider 无关的稳定前缀断点、不同 Provider 的协议适配和同前缀首请求预热后，使用同一知识库、同一文档重复运行，整体及分阶段缓存读取率均有提升。

| 范围 | 9 月 8 日冷运行 | 9 月 10 日冷运行 | 冷运行提升 | 9 月 8 日热运行 | 9 月 10 日热运行 | 热运行提升 |
|---|---:|---:|---:|---:|---:|---:|
| 全部 Wiki 模型调用 | 14.78% | 17.19% | **+2.41 pp** | 16.46% | 24.97% | **+8.51 pp** |
| Wiki 页面生成 | 19.19% | 19.99% | **+0.80 pp** | 20.95% | 21.49% | **+0.54 pp** |
| Wiki 引用抽取 | 8.27% | 14.86% | **+6.59 pp** | 9.83% | 15.34% | **+5.51 pp** |

`pp` 表示百分点。缓存读取率统一按 `cache_read_tokens / prompt_tokens` 计算；`prompt_tokens` 包含缓存读取、显式缓存写入和未命中 Token。

## 如何解释结果

1. **Wiki 页面生成有提升，但幅度较小。** 页面生成原来已经获得一部分服务商隐式缓存，加入显式稳定前缀后，热运行增加 0.54 个百分点。
2. **引用抽取是本轮最明确的收益。** 将固定规则和候选节点放在稳定消息中、将变化的 chunks 放在动态消息中后，热运行增加 5.51 个百分点。
3. **整体热运行增加 8.51 个百分点，不能全部归因于显式断点。** 同轮候选抽取还在服务商五分钟窗口内获得 99.51% 隐式缓存命中，因此汇报直接效果时应优先引用页面生成和引用抽取两行。
4. **结果证明的是当前服务商和固定工作负载下的实测改善。** 方案本身不检查模型名称，但不同 Provider 的缓存能力、计费和有效期不同，不能把该百分比直接外推到所有模型。

## 实测调用规模

- 每轮 28 次 Wiki 模型调用，28 次全部成功。
- 页面生成 16 次；引用抽取 9 次；其余为候选抽取等调用。
- 9 月 10 日冷运行：总输入 267,013 Token，缓存读取 45,894 Token。
- 9 月 10 日重复运行：总输入 266,116 Token，缓存读取 66,456 Token。
- 页面稳定前缀 2,370 Token：冷运行首请求写入，后续 15 次读取；重复运行 16 次全部读取。
- 引用稳定前缀约 1,293–1,343 Token：每轮首批建立缓存，随后 8 个 chunk 批次读取。

## 证据链

### 9 月 8 日参考数据

- `wiki-cache-calls.csv`：逐调用脱敏数据。
- `wiki-cache-summary.json`：冷/热运行聚合数据。
- `wiki-cache-report.md`：当时阶段的解释。

### 9 月 10 日最终版本

- `../wiki-generation-cache-2026-09-10/final-cold-calls.csv`
- `../wiki-generation-cache-2026-09-10/final-warm-calls.csv`
- `../wiki-generation-cache-2026-09-10/final-*-rerun.log`
- `../wiki-generation-cache-2026-09-10/summary.json`
- `../wiki-generation-cache-2026-09-10/result.md`

`summary.json` 的 `metadata.before_source` 明确指向 9 月 8 日汇总，六组 comparison 同时保存前值、后值、百分点变化和相对变化。原始数据没有被覆盖，可以继续复查。

## 验收方法

1. 确认后端使用最终 Tag `rhino-2026-final-3`。
2. 使用同一租户、知识库、文档和模型，连续执行两轮 Wiki 重跑。
3. 以每轮 UTC 开始、结束时间筛选 `model_call_records`。
4. 分别按全部 Wiki 调用、`wiki_page_modify`、`wiki_chunk_citation` 聚合。
5. 计算 `SUM(cache_read_tokens) / SUM(prompt_tokens)`，并确认调用全部成功。
6. 与本表比较。服务商缓存具有时间窗口，数值允许波动，但逐调用记录应能看到稳定前缀“一次写入、后续读取”的模式。
